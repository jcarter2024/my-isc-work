#mutable objects can be subject to aliasing 
#when we set two variables equal (as in a list) if we change one it can change the other

#we can avoid aliasing using import copy

#python's copy.deepcopy to replicate an object 

