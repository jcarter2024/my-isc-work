#strings compare from left to right 
#note strings will compare stringed numbers incorrectly i.e '3' < '90' = True
#Strings are immutable so we cannot alter individual characters etc
#We can concatonate strings with + 

#curly braces allow us to build strings by position 
#'{0}, {1}, {2}'.format('a', 'b', 'c') = a b c


#perc_yield = 12.4
#print('yield: {:6.2f}'.format(bla))

#f-strings
#name = 'andy'

#print(f"hello {name}")    signifies f string to look inside for curly braces
#this allows us to do mathematics within the string

#print(f"you are {some math}!")

#remember \ for inserting apostrophes
#triple quotes are useful for documentation, this auto interprets the new lines as \n




